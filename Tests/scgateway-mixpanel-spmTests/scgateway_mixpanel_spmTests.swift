import XCTest
@testable import scgateway_mixpanel_spm

final class scgateway_mixpanel_spmTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(scgateway_mixpanel_spm().text, "Hello, World!")
    }
}
